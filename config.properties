# For local Android emulator (uncomment when using Android):
platform=android
# For local iOS Simulator (uncomment when using iOS; not currently supported by this project's scripts):
#platform=ios
appiumServerUrl=http://localhost:4723

# Notification Settings
disableNotificationsBeforeLaunch=true

# Test login credentials — moved to test-data/production.properties and
# test-data/staging.properties (one file per backend environment, each with
# a `default` entry plus any named test users selectable via /run's optional
# 4th argument). See application/test-data.md.
