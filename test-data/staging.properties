# Test credentials for the Staging backend/data set (the in-app login
# screen's Staging/Production toggle set to Staging — see
# application/environments.md).
#
# Each named block below is selected with `/run <flow> <apk> Staging <name>`
# (the 4th, optional argument) and must provide at least
# `<name>MobileNumber` and `<name>Password`; `<name>UserCode` is optional.
# There is no `default*` entry in this file, so the 4th argument is
# effectively required for Staging runs — omitting it fails with a list of
# the names defined here. Do not mix these values with
# test-data/production.properties — each file is scoped to one backend only.

testUserOneMobileNumber=9355098542
testUserOnePassword=Weye@1234
